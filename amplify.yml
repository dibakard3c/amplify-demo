version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps --save-dev
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
